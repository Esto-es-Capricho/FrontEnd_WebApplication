import { SellerConsumersEntity } from './seller-consumers.entity';

describe('SellerConsumersEntity', () => {
  it('should create an instance', () => {
    expect(new SellerConsumersEntity()).toBeTruthy();
  });
});
